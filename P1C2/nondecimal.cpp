// Prints integers to the screen in their
// octal and hexadecimal representations.
#include <cstdio>
int main() {
    unsigned int a = 100;
    printf("Value of a in decimal: %d\nValue of a in binary: %b\nValue of a in octal: %o\nValue of a in hexadecimal: %x", a, a, a);
}